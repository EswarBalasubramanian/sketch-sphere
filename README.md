# sketch-sphere
Whiteboard App is a drawing application made using Angular. It allows users to draw freely on the screen using mouse. We can change the stroke color and width. I used reactive forms (FormControlName) instead of ngModel and handled the canvas mouse events inside a directive for better structure.
